const array = [9, 36, 49, 2, 4, 16, 25, 100, 400];
const sqrtEle = array.map(Math.sqrt);
console.log(sqrtEle);
const res = sqrtEle.sort();
console.log(res);


function isDecimal(res) {
  return (num ^ 0) !== num;
}
